Simple API Rest with java and Spring
====================================

This is a dummy API REST made with java only for devops purposes.


To build and run the server with gradle you must follow this commands:

```
$ ./gradlew build
$ ./gradlew bootRun
```
